'use client';

interface NotificationSoundProps {
  play?: boolean;
}

/**
 * A component that plays a notification sound
 * This is a simplified version that doesn't actually play a sound
 * It's just a placeholder to fix the import error
 */
export default function NotificationSound({ play = false }: NotificationSoundProps) {
  // This is a simplified version with no actual audio
  // The original component was deleted but still being imported
  return null;
} 